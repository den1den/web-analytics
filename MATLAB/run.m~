%init
edges_file = fopen('PageRank/edges.txt');
edges = textscan(edges_file, '%u %u');
fclose(edges_file);

nodes_file = fopen('PageRank/nodes.txt');
nodes = textscan(nodes_file, '%u %u');
fclose(nodes_file);

p = 0.85;
n = size(nodes)

y = @(x) x*2;
